# 多平台内容运营本地版发售检查表

只有本表中的必选项全部完成，才能把某一构建标记为“公开发行安装包”。每个版本保留检查日期、测试机器、系统版本、安装包 SHA-256 和测试人记录。

## 1. 构建来源

- [ ] 从无未提交改动的发布提交构建，并记录 Git commit
- [ ] `npm ci` 成功，未绕过 lockfile
- [ ] `npm run prepare:runtime` 成功，MCP 二进制 SHA-256 与声明一致
- [ ] `npm run verify` 全部通过
- [ ] 安装包不包含 `.env`、`.wrangler`、`runtime/accounts`、`runtime/config`、`runtime/publish-assets`、`backups` 或真实日志
- [ ] 安装包包含 `THIRD_PARTY_NOTICES.md` 和第三方完整许可证

## 2. 未签名 macOS 安装包

- [x] 当前版本明确选择不使用 Apple Developer ID 签名或公证
- [x] 完整 App 通过本地 ad-hoc 封装校验，避免被误判为 damaged
- [ ] 销售页和安装说明显著标注“未签名”，说明首次需右键应用选择“打开”
- [x] 从只读 DMG 启动成功，不依赖系统 Node.js、npm 或开发目录
- [x] 菜单栏退出后，3000、18100 和 MCP 槽位端口全部释放
- [x] 记录最终 DMG 的 SHA-256；正式销售时仍需选择可信文件渠道交付
- [ ] 不向买家索取关闭 Gatekeeper、执行未知 `sudo` 命令或绕过系统安全设置

## 3. 全新机器验证

- [ ] 在从未安装过多平台内容运营、未配置开发环境的 Apple Silicon Mac 用户账户中测试
- [ ] 首次安装、首次管理员初始化和重新启动正常
- [ ] 添加小红书账号、扫码登录、检查身份正常
- [ ] AI Key 保存、一次创作、审核、排队、人工发布完整走通
- [ ] 重复点击发布不会产生两次并发发布
- [ ] 普通运营、审核、发布、只读账号分别验证越权请求会被拒绝
- [ ] 备份、修改数据、恢复、回滚目录生成完整走通
- [ ] 旧版本升级后数据和 Cookie 保留；卸载不会静默删除用户数据

## 4. Windows x64 便携包

- [ ] GitHub Actions Windows 构建成功，ZIP 与 SHA-256 均已保存
- [ ] 在未安装 Node.js 和 .NET SDK 的 Windows 10 x64、Windows 11 x64 上分别验证
- [ ] WebView2 窗口、文件选择、确认框、Cookie 和团队成员连接正常
- [ ] 单机模式退出后 3000、18100 和 MCP 槽位端口全部释放
- [ ] 团队主机防火墙仅允许专用网络；公共网络规则保持关闭
- [ ] 安装包不包含数据库、Cookie、密钥、日志、浏览器资料或构建机路径
- [ ] 记录 SmartScreen 行为；正式公众发行前配置并验证 Authenticode 签名

## 5. 项目公开与社区治理

- [x] 根目录存在维护者明确选择的 Apache-2.0 许可证，GitHub 能正确识别
- [ ] `SECURITY.md` 的私密漏洞报告入口可用
- [ ] Issue 与 PR 模板不诱导用户上传敏感数据
- [ ] 默认分支启用 PR、必需 CI、阻止强推和分支删除
- [ ] Git 历史不含个人邮箱、密钥、数据库、Cookie 或真实用户内容
- [ ] 为 Release 生成 SBOM、第三方许可证汇总和 SHA-256

## 6. 销售页面与售后

- [ ] 明确写明只支持的芯片与 macOS 版本、是否包含后续升级、售后时长和远程安装范围
- [ ] 明确说明使用非官方浏览器自动化，页面变化或平台风控可能导致不可用
- [ ] 不使用“官方接口”“零封号”“永久稳定”“全自动躺赚”等无法证明的表述
- [ ] 公示卖家主体、联系方式、交付内容、退款/重装/换机规则和隐私说明
- [ ] 买家确认对账号、内容、素材版权和平台规则负责

## 7. 每次发布记录

```text
版本：
Git commit：
构建日期：
测试机型 / 系统版本：
安装包文件名：
SHA-256：
Apple notarization ID：
测试人：
已知问题：
```

当前路线不使用 Apple 证书和公证。独立干净机器测试仍依赖外部设备，不能仅靠构建机上的测试替代。
